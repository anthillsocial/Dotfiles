# List snapper configerations
snapper list-configs

# List the root (/) filesystem snapshots
sudo snapper -c root ls 

# List the home (/home) filesystem snapshots
sudo snapper -c home ls

# List the media (/mnt/media) filesystem snapshots
sudo snapper -c media ls

